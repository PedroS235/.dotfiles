-- Personal window rules. See https://wiki.hypr.land/Configuring/Basics/Window-Rules/

-- o.window("^(1Password)$", { float = true })

-- o.window("org.gnome.Nautilus", { float = true, size = { "50%", "70%" } })

-- No properties were set on this rule in the old config, so it currently has no effect.
o.window("org.gnome.Calculator", {})

o.window("org.gnome.Evince", { float = false, maximize = true })

o.window("Emulator", { float = true })
